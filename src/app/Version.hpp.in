#pragma once

// This header is generated by CMake. Keep the canonical version string on a
// single line so consumers that grep for it (for example the UA builder) can
// find one unambiguous source of truth.
#define BOORUBOX_VERSION_MAJOR @PROJECT_VERSION_MAJOR@
#define BOORUBOX_VERSION_MINOR @PROJECT_VERSION_MINOR@
#define BOORUBOX_VERSION_PATCH @PROJECT_VERSION_PATCH@
#define BOORUBOX_VERSION_STRING "@PROJECT_VERSION@"
